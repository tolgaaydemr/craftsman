using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class panelHidden : MonoBehaviour
{

    public Canvas Test;

    

    
    
   
    // Start is called before the first frame update
    void Start()
    {
        //Test.enabled = true;
    }

    // Update is called once per frame
    void Update()
    {
            
    }
}
